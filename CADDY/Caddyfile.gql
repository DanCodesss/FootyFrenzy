footyfrenzy.us.to {
    reverse_proxy {
        to localhost:8080  # Adjust the port if your website is running on a different port
    }
    tls your_email@example.com  # Replace with your email address
}
